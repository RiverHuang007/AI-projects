## 第一步：请告诉我你现在的准备情况

请回答以下三个问题，答案无所谓好坏，只是让我知道从哪里带你起步：

1. **电脑系统**：你用 Windows 还是 Mac？答案：Mac电脑
2. **有没有装过任何编程相关的软件**：比如 VS Code、Node.js、Python、Git 之类的？（如实说就好，完全没装过是最省事的）答案：都已经装好
3. **今天能拿出多少连续时间**：比如接下来1小时、今晚2小时、还是周末半天？答案：每天1小时

---

## 根据你的回答，我会立即启动对应路线

如果你回答“完全没装过任何东西”，那我们的第一节课就是：

**第0步（15分钟）——装好三样东西，让你看到第一个页面：**

1. 装 VS Code（写代码的地方）
2. 装 Node.js（让代码能跑起来的引擎）
3. 装 Git（后悔药，随时回到上一秒）

**第1步（30分钟）——用Cursor写出你的第一个网页：**

- 打开 Cursor，我说一句中文需求，你照打进去
- 点击运行，浏览器里出现你的页面
- 你亲手改一个字，页面立刻变

全程我在旁边告诉你每一步点什么按钮、敲什么字，不会让你自己猜。
*这里需要的软件，Opencode/VS code/Node.js/Git/Cursor，如果没有安装好直接用Opencode安装即可*

---

## 等你有了第一个项目，我们再逐步加

|阶段|任务|我的角色|
|---|---|---|
|第1次|装环境 + 第一个网页|我说你敲，你看着它跑起来|
|第2次|用OpenCode给网页加功能|我告诉你什么时候切到终端、输什么命令|
|第3次|把踩坑记到Obsidian|我告诉你记什么格式、怎么分类|
|第4次|部署到Vercel让别人能访问|我带你点每一步按钮|

每个阶段我只带你做一个具体的、看得见摸得着的东西，不讲理论课。

## 今天第1小时：用Cursor写出你的第一个网页并上线

### 目标

30分钟内写出一个能打开的网页，再用30分钟把它部署到网上，任何人点链接都能看到。

---

### 操作步骤（跟着我做，一步都不要跳）

#### 第一步：打开 Cursor（5分钟）

1. 在 Mac 的「启动台」或「应用程序」里找到 **Cursor**，双击打开
2. 如果是第一次打开，它会问你是否导入 VS Code 设置，选 **跳过**​ 或 **不导入**
3. 你会看到一个欢迎界面，**直接关掉**那个欢迎标签页
4. 现在你应该看到一个空白的窗口，左边是文件区，右边是大片空白

✅ **做到了吗？**​ 如果看到空白窗口，继续往下。如果卡住了，告诉我卡在哪一步。

---

#### 第二步：创建一个新项目（5分钟）

1. 在 Cursor 顶部菜单点击 **File → New Folder**（或者按 `Cmd+Shift+N`）
2. 在弹出的框里，输入项目名字：`my-first-site`
3. **选择一个你记得住的位置**，比如「桌面」或「文稿」
4. 点击「创建」

现在 Cursor 左边的文件区应该能看到一个空的文件夹 `my-first-site`。

✅ **做到了吗？**​ 左边看到空文件夹，继续。

---

#### 第三步：让 AI 帮你写出第一个网页（10分钟）

1. 在 Cursor 左边的文件区**右键点击空白处**，选择 **New File**
2. 输入文件名：`index.html`（注意大小写，必须是 index.html）
3. 回车确认，现在文件区出现了一个 `index.html`
4. **最关键的一步**：按下键盘快捷键 **`Cmd+I`**（Mac 的 Command + I）
5. 屏幕中间会出现一个输入框（这是 Cursor 的 AI 对话窗）
6. 在输入框里**一字不差地输入**下面这句话：

纯文本

纯文本

```
帮我写一个个人主页，包含：
- 一个大标题显示我的名字（名字暂时用"访客"）
- 一段自我介绍
- 三个技能标签（HTML、CSS、JavaScript）
- 一个联系按钮
- 背景颜色用浅蓝色渐变
- 所有内容居中显示，风格简洁干净
```

1. 按回车发送

等待几秒钟，你会看到 Cursor 自动在 `index.html` 文件里写出一大段代码。

✅ **做到了吗？**​ 文件里有代码了，继续。

---

#### 第四步：在浏览器里看到你的网页（5分钟）

1. 在 Cursor 的文件区，**右键点击**​ `index.html`
2. 选择 **Reveal in Finder**（在访达中显示）
3. 在打开的 Finder 窗口中，**双击**​ `index.html`
4. 你的默认浏览器会自动打开，显示出你的第一个网页！

你应该能看到一个漂亮的个人主页，有标题、介绍、技能标签和按钮。

✅ **看到了吗？**​ 如果页面正常显示，恭喜你，你已经写出了第一个网页。如果没显示或报错，告诉我你看到的提示。

---

#### 第五步：部署到网上，任何人都能访问（剩余时间）

这一步我们用 **Vercel**，它是免费的，并且和你 Mac 的终端配合很好。

1. 打开浏览器，访问 vercel.com
2. 点击右上角 **Sign Up**（注册）
3. 选择 **Continue with GitHub**（用 GitHub 账号登录）
    - 如果你还没有 GitHub 账号，点击「Sign up for GitHub」，用邮箱注册一个（5分钟搞定）
4. 授权 Vercel 访问你的 GitHub 账号
5. 登录成功后，你会看到 Vercel 的控制台页面
6. **先停在这里，明天第2小时我们从这里继续部署**

---

### 今天的成果

- ✅ 安装了 Cursor
- ✅ 创建了第一个项目文件夹
- ✅ 用 AI 写出了第一个网页
- ✅ 在浏览器里看到了自己的页面
- ⏳ 部署部分明天完成

---

### 今天结束后，请你做一件事

打开 **Obsidian**，新建一篇笔记，标题为 `2026-09-06-第一天-第一个网页`，在里面写下：

1. 今天做了什么（一句话） - 安装了软件：Cursor，obsidian，并生成了网页
2. 过程中有没有遇到卡住的地方 - 没有卡点
3. 你觉得哪个步骤最有趣 - 建立网页比较有趣，期待完成自己的主页

这不是作业，是给你自己留的记录。以后回头看会很有成就感。